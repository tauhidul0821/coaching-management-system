import React from 'react';

const footerDesign: React.CSSProperties = {
    textAlign: 'center', 
};

const Footer: React.FC = () => (
    <footer style={footerDesign}>
        This is footer
    </footer>
);  
export default Footer
